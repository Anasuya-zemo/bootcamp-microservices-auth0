apiVersion: apps/v1
kind: Deployment
metadata:
   name: patient-service-deploy
   labels:
      type: patient-svc
spec:
  template:
    metadata:
      name: patient-pod
      labels:
        type: patient-svc
    spec:
      containers:
      - name: patient-service-image
        image: ghcr.io/anasuya-zemo/patient-service:latest
        ports:
        - containerPort: 9005
        envFrom:
          - secretRef: 
              name: database-secret
        env:
          - name: eureka.client.service-url.defaultZone
            value: https://backend.spcluster.tk/eureka/
          
          - name: eureka.instance.hostname
            value: patient 
            
          - name: spring.datasource.url
            value: jdbc:mysql://3.83.138.1:30001/zemoso_diagnostics?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      imagePullSecrets: 
      - name: ghcr-secret

  replicas: 1
  selector:
    matchLabels:
      type: patient-svc
