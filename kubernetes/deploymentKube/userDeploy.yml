apiVersion: apps/v1
kind: Deployment
metadata:
   name: user-service-deploy
   labels:
      type: user-svc
spec:
  template:
    metadata:
      name: user-pod
      labels:
        type: user-svc
    spec:
      containers:
      - name: user-service-image
        image: ghcr.io/anasuya-zemo/user-service:latest
        ports:
        - containerPort: 9008
        envFrom:
          - secretRef: 
              name: database-secret
        env:
          - name: eureka.client.service-url.defaultZone
            value: https://backend.spcluster.tk/eureka/
          
          - name: eureka.instance.hostname
            value: address

          - name: spring.datasource.url
            value: jdbc:mysql://3.83.138.1:30001/zemoso_diagnostics?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      imagePullSecrets: 
      - name: ghcr-secret

  replicas: 1
  selector:
    matchLabels:
      type: user-svc
