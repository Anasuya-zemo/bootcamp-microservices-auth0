apiVersion: apps/v1
kind: Deployment
metadata:
   name: lab-service-deploy
   labels:
      type: lab-svc
spec:
  template:
    metadata:
      name: lab-pod
      labels:
        type: lab-svc
    spec:
      containers:
      - name: lab-service-image
        image: ghcr.io/anasuya-zemo/lab-service:latest
        ports:
        - containerPort: 9004
        envFrom:
          - secretRef: 
              name: database-secret
        env:
          - name: eureka.client.service-url.defaultZone
            value: https://backend.spcluster.tk/eureka/
          
          - name: eureka.instance.hostname
            value: lab

          - name: spring.datasource.url
            value: jdbc:mysql://3.83.138.1:30001/zemoso_diagnostics?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      imagePullSecrets: 
      - name: ghcr-secret

  replicas: 1
  selector:
    matchLabels:
      type: lab-svc
