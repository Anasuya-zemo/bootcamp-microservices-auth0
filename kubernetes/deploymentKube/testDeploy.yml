apiVersion: apps/v1
kind: Deployment
metadata:
   name: test-service-deploy
   labels:
      type: test-svc
spec:
  template:
    metadata:
      name: test-pod
      labels:
        type: test-svc
    spec:
      containers:
      - name: test-service-image
        image: ghcr.io/anasuya-zemo/test-service:latest
        ports:
        - containerPort: 9007
        envFrom:
          - secretRef: 
              name: database-secret
        env:
          - name: eureka.client.service-url.defaultZone
            value: https://backend.spcluster.tk/eureka/
          
          - name: eureka.instance.hostname
            value: address

          - name: spring.datasource.url
            value: jdbc:mysql://3.83.138.1:30001/zemoso_diagnostics?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      imagePullSecrets: 
      - name: ghcr-secret

  replicas: 1
  selector:
    matchLabels:
      type: test-svc
